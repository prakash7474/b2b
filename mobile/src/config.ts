/**
 * App configuration.
 *
 * The Flask backend runs on your development machine. On a phone/emulator,
 * `localhost` means the device itself, so you must use your machine's LAN IP
 * (e.g. 192.168.1.10) instead of `localhost`.
 *
 * Example: export const API_BASE_URL = 'http://192.168.1.10:5000';
 */
export const API_BASE_URL = 'http://localhost:5000';
